@model List<LanguageViewModel>

@{
    ViewData["Title"] = "LanguageIndex";
}

<h1>LanguageIndex</h1>

<a asp-action="ReturnToPeopleIndex">Return to Main Menu</a>
   <p></p>
   <a asp-action="AddLanguageToPerson">Add language to a person</a>

    <div class="col-lg-6">
    <table class="table">
    <thead>
        <tr>
            <th>Language</th>
            <th>Language short</th>   
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.LanguageName</td>
                <td>@item.LanguageShortName</td>
                <td>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>
