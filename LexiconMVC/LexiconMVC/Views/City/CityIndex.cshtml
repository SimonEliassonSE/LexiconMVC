

@{
    ViewData["Title"] = "CityIndex";
}


<h1>CityIndex</h1>
   <a asp-action="ReturnToPeopleIndex">Return to Main Menu</a>
   <a asp-action="AddPersonToCity">Add person to city</a>
@*    <div class="col-lg-6">
    <table class="table">
    <thead>
        <tr>
            <th>CityPostalCode</th>
            <th>City Name</th>   
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.CityPostalCode</td>
                <td>@item.CityName</td>
                <td>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>*@

    

